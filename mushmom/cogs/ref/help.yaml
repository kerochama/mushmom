# custom help info

characters:
  rename:
    sigs:
      - "[name] <new_name>"

emotes:
  emote:
    options: &imflags
      - "--[name]"

import:
  import:
    sigs:
      - "<name> [url: maplestory.io]"
      - <name> with JSON file attached

sprites:
  sprite:
    options: *imflags
