# custom signature info
# aliases here are always listed, even for hidden commands
# aliases can also have spaces

characters:
  rename:
    sigs:
      - "[name] <new_name>"

core:
  _set_prefixes:
    aliases: ['set prefixes']
  _set_channel:
    aliases: ['set channel']

import:
  import:
    sigs:
      - "<name> [url: maplestory.io]"
      - <name> with JSON file attached
